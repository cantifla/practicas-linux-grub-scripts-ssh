# practicas-linux-grub-scripts-ssh
rácticas básicas de administración en Linux: configuración de GRUB, scripts en Bash para backups y red, y configuración de red en VM con acceso por SSH desde Windows.
